version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - echo "Build started on `date`"
      - npm ci --only=production
      - echo "Build completed on `date`"
run:
  runtime-version: 16
  command: node server.js
  network:
    port: 3002
    env: PORT
  env:
    - name: NODE_ENV
      value: production
